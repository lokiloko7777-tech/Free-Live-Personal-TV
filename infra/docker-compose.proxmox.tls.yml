services:
  app:
    environment:
      - REQUIRE_HTTPS=1

  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.tls.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro